export default function Statistics({ statistics, alerts }) { const cells = [['Total', statistics.total_detections], ['Bottles', statistics.plastic_bottles], ['Bags', statistics.plastic_bags], ['Cans', statistics.aluminium_cans], ['Alerts', alerts]]; return <section className="stats">{cells.map(([label, value]) => <div key={label}><small>{label}</small><strong>{value ?? 0}</strong></div>)}</section> }
